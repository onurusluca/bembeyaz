{
  "name": "bembeyaz",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/core build",
    "dev": "vite packages/core --host --open /demo/dev.html",
    "dev:core": "bun run --cwd packages/core dev",
    "demo": "bun run --cwd packages/core build && bunx serve packages/core -p 4173"
  },
  "devDependencies": {
    "vite": "^8.0.3"
  }
}
